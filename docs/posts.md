# Posts for the ProPresenter Facebook group

Each post is under 80 words. No hype, no model names.

## 1. What it does

verse-cue watches the lyric slide that is already on screen and the vocal mic,
then hits Next a little before the last word. No song files to load. Network
API on, pick the input, run it.

https://github.com/AustinDKB/verse-cue

## 2. Windows testers (NVIDIA)

Looking for a church with ProPresenter on a Windows PC and an NVIDIA GPU who
will try verse-cue on a rehearsal, not a Sunday first. Send hardware, the
summary.md the bench writes, and whether it was early or late. Link above.

## 3. Mac testers

Same ask for Mac. CPU-only is expected to pick a smaller model. If Next is
late, that is useful data. Rehearsal only until we have a few reports.

## 4. After the first Sunday

`[fill after run]` We used verse-cue live. Median fire vs the last word:
`[n] s` early/late. Missed slides: `[n]`. Operator overrides: `[n]`. Repo
and numbers: https://github.com/AustinDKB/verse-cue
